alertlog=$(sqlplus -s / as sysdba << EOF | grep -v "^--" 
set pages 0 lines 120
col name format a20
col value format a80
select value from v\$diag_info where name='Diag Trace';
EOF
)
cd ${alertlog}
